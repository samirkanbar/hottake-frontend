import { View, Text, ScrollView } from 'react-native'
import React from 'react'

const index = () => {
  return (
    <ScrollView>
      <Text>tommy</Text>
    </ScrollView>
  )
}

export default index