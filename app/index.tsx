import { View, Text } from 'react-native'
import {SafeAreaView} from 'react-native-safe-area-context';
import React from 'react'

const index = () => {

    const numActivities = 0;

    return (
        <SafeAreaView>
            <Text>hello world</Text>
        </SafeAreaView>
    )
}

export default index